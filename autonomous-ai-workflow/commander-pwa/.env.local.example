# Antigravity Commander PWA — 環境変数テンプレート
# cp .env.local.example .env.local

# MCP サーバー（Python FastMCP）のURL
# ローカル開発時: notebooklm-mcp-server を起動してから設定
MCP_SERVER_URL=http://localhost:8080

# 承認 Webhook シークレット（mcp-server/.env と同じ値を設定）
APPROVAL_WEBHOOK_SECRET=change-me-in-production
